<view bind:tap="onGoToDatail" class="da">
  <image class="movie" src="{{movie.images.large}}"></image>
  <text class="title">{{movie.title}}</text>
  <view class="pf">
  <l-rate active-color="yellow" disabled="{{true}}" size="22" score="{{movie.rating.stars/10}}" />
  <text class="pfsz">{{movie.rating.average}}</text>
  </view>
</view>
